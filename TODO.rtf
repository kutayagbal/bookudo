Refactor code
Get title and content from image (Image to text). Can be done Partly. One digit numbers are being omitted.
Daily goals ?
Delete accidential progress (Progress list view) ?
Get image page no from image itself (Image to text). Can be done Partly. One digit numbers are being omitted.
Save text only for images. Otherwise 1MB storage is required per image (Image to text)
Let user highlight lines before saving images. Maybe letting them to draw rectangle is enough (Image to text)
Let user projectile back and forth for graphs.
